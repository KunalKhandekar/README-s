## 1. React Fundamentals & Basics

- What is React.js and how it is different from other frameworks/libraries?
- What is Single Page Application?
- Where else React is used?
- Why component should start with capital letters?
- Why it is recommended to create different components? Benefits?
- What are the different types of components in React.js?
- What is the difference between class and functional components?
- What is React Pure Component? Explain roles and use cases

---

## 2. React DOM & Rendering

- What is the difference between React and React DOM?
- Why React does not perform DOM operations directly?
- Why ReactDOM.render() is not supported after React 18?
- How re-render happens when state changes in React?
- What happens if you try to update the state directly in React?
- How React detects changes?
- What is react batching?

---

## 3. JSX & Rendering Logic

- What is JSX?
- Difference between JSX and TSX
- How can we write JavaScript expressions inside JSX?
- What things or data types can be written inside JSX?
- Can we wrap child elements in an array?
- `<App />` or `App()` which one is valid inside React?
- Why `.jsx` filename is important?

---

## 4. React Elements & Components

- What is React Element?
- What is React Component?
- Difference between React Element and React Component

---

## 5. State & Props

- What is state in React?
- What are props?
- Difference between props and state
- What is props drilling?
- What are alternatives to props drilling?
- How to share data among sibling components?
- Difference between useState and useRef in form handling
- Difference between useState, useContext, and useReducer

---

## 6. Lifecycle & Side Effects

- Explain lifecycle methods of class components
- How do functional components handle lifecycle methods?
- How to handle side effects in class and functional components?
- How can we implement componentWillUnmount in functional component?
- What is the difference between useEffect and useLayoutEffect?

---

## 7. Hooks

- What are React hooks?
- List hooks you have used
- What was the pain point of state management and what useState solved?
- What is useEffect?
- What pain point does useEffect solve?
- What happens if there is no dependency array?
- What is the use of dependency array?
- What can useEffect return?
- What is the cleanup function?
- What is useRef? Use case
- What is useMemo? Use case
- What is useCallback? Use case
- Difference between useCallback and useMemo
- What are the rules of hooks?
- What are custom hooks?
- Have you used custom hooks in a project? Example

---

## 8. Virtual DOM & Reconciliation

- What is Virtual DOM?
- What is Shadow DOM?
- Difference between Real DOM and Virtual DOM
- Explain difference between real and virtual DOM elements
- How does React compare Virtual DOM and Real DOM?
- How Virtual DOM compares primitive and non primitive data types?
- What is diffing algorithm?
- Does it change the whole element or only the node?
- What is reconciliation process?
- What is React Fiber?
- How is React Fiber different from previous process?

---

## 9. Keys & Lists

- Why keys are important in dynamic elements?
- Why using index as key is not recommended?
- What is virtualization in React? What is pagination? What's the difference?
- How do you optimize rendering of large lists?

---

## 10. Performance Optimization

- What is React.memo?
- How does React.memo work?
- When to use React.memo?
- How to avoid unnecessary re-rendering?
- How do you optimize a React application for performance?
- How do you identify and fix memory leaks in React?
- Web Vitals and performance metrics
  - LCP
  - CLS
  - INP
  - TTFB

---

## 11. Styling & Assets

- What is styled-components?
- Have you implemented theming in React?
- Why do we store media files in public folder and access using relative path?

---

## 12. Forms & Validation

- Difference between controlled and uncontrolled components
- How do you manage form validation in React?
- How do you optimize forms with hundreds of input fields?
- How would you implement a multi-step form with conditional rendering?

---

## 13. Events

- What is a synthetic event in React?
- Difference between synthetic event and real DOM event
- How to pass arguments to event handlers?

---

## 14. Routing & Code Splitting

- How do you handle dynamic routing in React?
- How do you implement lazy loading with routes?
- What is portal in React?
- What are fragments in React? Why are they used?

---

## 15. Data Fetching & APIs

- How do you fetch data in React? Examples
- What is Axios?
- Difference between Axios and fetch API
- How do you handle errors in async operations?
- How do you implement retry logic for failed API requests?
- How do you securely store API keys in React?

---

## 16. Context API & State Management

- What are Context API hooks?
- Explain Context API with example
- Difference between Context API and Redux
- How would you implement global state without Redux?

---

## 17. Redux

* Why do we need Redux if we already have Context API?
* What is Redux?
* What are the three core principles of Redux?
* Why do we say Redux is predictable?
* What is reducer, action, and store?
* Explain data flow in Redux
* What is Redux Toolkit?
* How does Redux Toolkit simplify Redux?
* What is the role of Immer library in Redux Toolkit?
* What are middleware in Redux?
* What is Redux Thunk?
* What is Redux Saga?
* Difference between Redux Thunk and Redux Saga
* Redux Thunk vs RTK Query
* createAsyncThunk() vs createApi()

## 18. Build Tools & Environment

- What is Vite?
- Does Vite use Babel?
- What is ESBuild?
- What is Babel?
- Is Babel used in production today?
- Difference between compiler and transpiler
- What is node_modules?
- Difference between package.json and package-lock.json
- Explain versioning in package.json
- Major difference between `Webpack` and Babel
- How do you configure Webpack for React?
- How do you build React applications for production?
- Purpose of `.env` file in React

---

## 21. React Internals & Miscellaneous

- What is super, constructor, and render function in React?
- Default localhost port in React and how to change it?
- What is CORS?
- What are CORS rules?

---

## 19. Strict Mode & Errors

- What is React.StrictMode?
- What is useStrict in React?
- What is the purpose of error boundaries?
- How do you handle errors in React?

---

## 20. Server Components

- What is react server components?

---
