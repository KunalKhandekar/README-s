# HTML Interview Preparation Questions

1. What is HTML?
2. What is the role of HTML in web development?
3. What is the difference between HTML and HTML5?
4. Why do we use `<!DOCTYPE html>`?
5. What happens if the doctype is missing?
6. What is the basic structure of an HTML document?
7. What are tags and elements?
8. What are empty or void elements?
9. Is HTML case-sensitive?
10. What are comments in HTML?
11. What are attributes in HTML?
12. What are global attributes?
13. What is the difference between `id` and `class`?
14. Can multiple elements share the same `id`?
15. What is the `data-*` attribute?
16. What is the `title` attribute used for?
17. Difference between `div` and `span
18. Difference between `strong` and `b`
19. Difference between `em` and `i`
20. How many `h1` tags should a page have and why?
21. Why should we avoid using HTML tags for styling?
22. What is the anchor (`a`) tag used for?
23. Difference between absolute and relative URLs
24. What is the purpose of the `alt` attribute in images?
25. What happens if an image does not load?
26. How do you open a link in a new tab?
27. How do you make an image clickable?
28. Difference between `ul`, `ol`, and `dl`
29. What is a nested list?
30. Difference between `th` and `td`
31. Why use `thead`, `tbody`, and `tfoot`?
32. How do tables impact performance and accessibility?
33. What is the purpose of the `form` tag?
34. Difference between `GET` and `POST` methods
35. Why is the `name` attribute important in forms?
36. Difference between `readonly` and `disabled`
37. How do radio buttons work?
38. What are different input types in HTML5?
39. How does basic HTML form validation work?
40. What is semantic HTML?
41. Name some semantic HTML tags
42. Difference between `section` and `article`
43. Difference between `div` and semantic elements
44. How does semantic HTML help SEO?
45. Difference between `audio` and `video` tag
46. What is the use of the `source` tag?
47. Why should autoplay be avoided?
48. What is web accessibility?
49. What are ARIA attributes?
50. What are some HTML best practices for performance and maintainability?
