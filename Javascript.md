# JavaScript Interview Preparation

## 1. JavaScript Basics & History

* What is JavaScript? When was it born?
* Why is JavaScript called a scripting language?
* Why JavaScript was chosen over other languages for the web?
* Is JavaScript compiled or interpreted?
* Where does JavaScript run?
* Is JavaScript single-threaded?
* Is JavaScript single-threaded? One example to prove it

---

## 2. JavaScript Engine & Runtime Environment

* What is the role of the JavaScript engine?
* What is Web API? What actually API means here?
* What components are there in Web API?
* What is BOM?
* Why window object is not available in Node.js but in browser, if both use V8 engine?
* Do we have any window-like object in Node.js?
* Is there any common object to access window and global depending upon the environment?

---

## 3. Data Types & Variables

* Explain data types in JavaScript
* Difference between primitive and reference (non-primitive) types
* Why array in JavaScript is of object type?
* What is typeof null? Why?
* When should you use const?
* Difference between var, let, and const
* Why is var considered unsafe?

---

## 4. Type Conversion & Comparison

* What is type coercion? Explicit and Implicit
* What are truthy and falsy values?
* Difference between == and ===
* Why `null == undefined` is true and `null === undefined` is false?
* Why null and undefined convert to number and NaN in less than and greater than comparison?
* What is the sum of 0.1 + 0.2? Why? What is IEEE 754?
* How & and | operator behaves?
* Should we use Math.random() for OTP generation?

---

## 5. Control Flow Statements

* Difference between for and while loop
* What does break do?
* When should switch be preferred?

---

## 6. Functions Fundamentals

* Difference between function declaration and function expression
* What are arrow functions?
* How this behaves in arrow functions?
* What happens if a function does not return anything?
* What is default parameter?
* What is callback function?
* What is Higher Order Function?
* What are pure and impure functions?
* What is IIFE? Real use case?
* What is currying?

---

## 7. Scope & Execution Context

* What is scope in JavaScript?
* Difference between block scope and function scope
* What is hoisting?
* Are let and const hoisted?

---

## 8. Arrays & Objects

* What is the difference between slice() and splice()?
* What is spread operator?
* Difference between rest and spread operator
* How sort() works in JavaScript for arrays?
* What is the difference between forEach and map?
* Explain filter, map, reduce
* Explain Array mutating and non-mutating methods with examples
* What is Shallow Copy and Deep Copy?

---

## 9. ES6 Collections

* What is a Set?
* What is Map and how it is different from Object?

---

## 10. DOM & Browser Interaction

* What is DOM?
* What is window object in browser? Is it available in Node.js?
* In how many ways can you select an element in DOM?
* What is the difference between Array and NodeList?
* What are events in JavaScript?

---

## 11. Event Handling & Event System

* What is event bubbling?
* What is event capturing and how it is different from bubbling?
* What are the three phases of event propagation?
* How to remove eventListener?
* Does JavaScript listen to event listeners you attach to elements?
* What is the difference between stopPropagation and preventDefault?
* What is Event Delegation?

---

## 12. Asynchronous JavaScript & Event Loop

* If JavaScript is single-threaded, then how does it manage multiple tasks?
* What is Event Loop? How does it work?
* What is Callback Queue and Microtask Queue?
* Which tasks go into which queue? **-------------------**
* What is callback hell? Write a code example
* What is the problem with callback hell?

---

## 13. Promises & Async/Await

* What are promises? Explain three states of promises
* What is promise chaining?
* How do you handle errors in promises? When do errors occur?
* What is the difference between JSON and JavaScript Object?
* How promises solve the problem of callback hell?
* What is the real-life use of .finally() in promise chaining?
* What is async/await and why is it used?
* How async function is different from a normal function?
* Does JavaScript really wait when we use await for asynchronous code?
* Explain different types of Promise APIs and their behavior

---

## 14. this Keyword & Function Binding

* What is this keyword?**---------------**
* How this behaves in strict and non-strict mode? Why?
* Explain call, apply, bind
* Do we use call, apply, bind now? Real-life use case
* How this keyword works in arrow function and normal function?
* How this keyword behaves in DOM operations?

---

## 15. Object Oriented JavaScript

* What is Prototype?
* What is the difference between prototype and **proto**?
* What is class and why it is used?
* What is constructor and why do we use it?
* Why objects created using class instances do not show methods in console.log but manually created objects do?
* What are the four pillars of Object Oriented Programming?
* Explain Inheritance and its types
* What are getters and setters? Write an example
* What is the role of super keyword inside and outside constructor?

---

## 16. ------- Strict Mode & Environment ---------

* What is strict mode and non-strict mode? Examples
* When should we use strict mode?

---

## 17. Performance & Browser Internals

* What is repaint and reflow?
* What is Mutation Observer?----------------

---

## 18. Optimization Techniques

* What is debouncing? Create a polyfill for debouncing--------
* What is throttling? Create a polyfill for throttling--------

